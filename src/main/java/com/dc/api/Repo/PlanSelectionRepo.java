package com.dc.api.Repo;

import com.dc.api.Model.PlanSelection;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlanSelectionRepo extends JpaRepository<PlanSelection,Integer> {
}
